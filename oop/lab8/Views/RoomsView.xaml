<UserControl x:Class="HotelManagement.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HotelManagement"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <Button Content="Добавить" Click="AddRoom_Click" Margin="5" Width="80"/>
            <Button Content="Изменить" Click="EditRoom_Click" Margin="5" Width="80"/>
            <Button Content="Удалить" Click="DeleteRoom_Click" Margin="5" Width="80"/>
            <ComboBox Name="CategoryComboBox" Width="150" Margin="5"
                      SelectionChanged="CategoryComboBox_SelectionChanged"/>
            <Button Content="Поиск доступных" Click="SearchAvailable_Click" Margin="5" Width="120"/>
            <Button Content="Сброс" Click="ResetSearch_Click" Margin="5" Width="80"/>
        </StackPanel>

        <DataGrid Name="RoomsDataGrid" Grid.Row="1" Margin="5" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionChanged="RoomsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding RoomNumber}" Width="80"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="120"/>
                <DataGridTextColumn Header="Этаж" Binding="{Binding Floor}" Width="60"/>
                <DataGridTextColumn Header="Вместимость" Binding="{Binding Capacity}" Width="80"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=C}" Width="80"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                <DataGridTemplateColumn Header="Изображение" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Image}" Width="100" Height="60" Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>