<UserControl x:Class="HotelManagement.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HotelManagement"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <Button Content="Добавить" Click="AddUser_Click" Margin="5" Width="80"/>
            <Button Content="Изменить" Click="EditUser_Click" Margin="5" Width="80"/>
            <Button Content="Удалить" Click="DeleteUser_Click" Margin="5" Width="80"/>
            <TextBox Name="SearchTextBox" Width="200" Margin="5" 
                     TextChanged="SearchTextBox_TextChanged"/>
            <Button Content="Поиск" Click="SearchUsers_Click" Margin="5" Width="80"/>
        </StackPanel>

        <DataGrid Name="UsersDataGrid" Grid.Row="1" Margin="5" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionChanged="UsersDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Username}" Width="120"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                <DataGridCheckBoxColumn Header="Админ" Binding="{Binding IsAdmin}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>